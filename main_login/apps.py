# ============================================================================
# CONFIGURACIÓN DE LA APLICACIÓN MAIN_LOGIN
# ============================================================================

from django.apps import AppConfig


class MainLoginConfig(AppConfig):
    # Configuración de la app main_login
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'main_login'
