# ============================================================================
# CONFIGURACIÓN DE LA APLICACIÓN MAIN_HOME
# ============================================================================

from django.apps import AppConfig


class MainhomeConfig(AppConfig):
    # Configuración de la app main_home
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'main_home'
